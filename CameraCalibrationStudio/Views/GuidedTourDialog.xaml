<Window x:Class="CameraCalibrationStudio.Views.GuidedTourDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Guided Tour" Height="320" Width="480"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="{StaticResource AppBackground}" FontFamily="Segoe UI"
        ShowInTaskbar="False">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="StepLabel" Text="Step 1 of 6" Foreground="{StaticResource TextSecondary}" FontSize="11" FontWeight="SemiBold" Margin="0,0,0,10"/>
        <TextBlock x:Name="TitleText" Grid.Row="1" Text="Load a camera image" Foreground="{StaticResource TextPrimary}" FontSize="20" FontWeight="SemiBold" Margin="0,0,0,14"/>
        <TextBlock x:Name="BodyText" Grid.Row="2" Text="" Foreground="{StaticResource TextPrimary}" FontSize="13.5" TextWrapping="Wrap" VerticalAlignment="Top"/>

        <Grid Grid.Row="3" Margin="0,18,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Content="Skip" Click="Skip_Click" Style="{StaticResource ToolButton}" Width="90" Height="32"/>
            <StackPanel Grid.Column="2" Orientation="Horizontal">
                <Button x:Name="BackButton" Content="Back" Click="Back_Click" Style="{StaticResource ToolButton}" Width="90" Height="32" Margin="0,0,8,0"/>
                <Button x:Name="NextButton" Content="Next" Click="Next_Click" Style="{StaticResource ToolButton}" Width="130" Height="32"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
