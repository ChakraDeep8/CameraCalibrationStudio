bin/
obj/
publish/
dist/
*.user
.vs/
logo_source.png
*.zip
*.exe

# graft's local graph cache — regenerable, not committed (run `graft build`).
/graft/

# Detection weights are fetched at runtime, never committed — see README.
*.onnx
